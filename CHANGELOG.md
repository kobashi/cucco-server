# Changelog

## v1.0.0b9 — カードの配布・交換アニメーションの表示タイミング(試用版)

プレイ用クライアントのみの変更(サーバー再起動は不要)。

### 修正

- **手札が配布アニメーションの完了前に見えていた問題**: 山札から配るアニメーションが
  終わる前に自分のカードが表になっていた。配布の流れを物理的な卓に合わせた:
  各席にカードが**1枚ずつ裏向きで着地**し(これまでは全席のカードが最後に一斉に
  現れていた)、全部配り終わってから自分の手札だけが**その場で表に返る**
- **カンビオ(交換)で古いカードが席に残ったままだった問題**: 交換の飛行中も両者の
  席に元のカードが表示されつづけ、飛んでいくカードが「複製」に見えていた。交換の
  開始時に**両方の席を空にし**、着地してから新しいカードを表示するようにした。
  カードの交換なので飛行は双方向のまま(1枚ずつ、互いの席へ)
  - 飛ぶカードには**その席に実際にあったカード**を使う(効果で公開済みなら表向きの
    まま飛ぶ)。以前は常に裏向きの汎用カードだった
- **山札とのカンビオでも同様に**、引いたカードが空中にある間は席を空にし、着地して
  から新しいカードを表示するようにした
- **前のディールの早送りが次のディールに持ち越され、配布アニメーションが丸ごと
  省略されることがあった問題**: `fastForward()`が立てる`instant`フラグは
  キューが空になるまで解除されないため、ディール終盤の早送り(結果ペインの
  セーフティネットや`hurry()`の上限)が次のディールの配布ステップまで巻き込んで
  いた。ディールの境界で`queue.resume()`を呼び、新しいディールは必ず実演する
  ようにした(`clear()`もフラグを解除する)

### テスト

- ライブ検証(稼働中サーバーには触れず、ポート8799の使い捨てサーバー+bot3体):
  MutationObserverでスロットの中身と飛行ゴーストの出入りを記録し、
  **修正前**は交換の飛行中(2ゴースト・全区間)両席が古いカードを表示したままで
  あることを実測 → **修正後**は飛行開始と同時に両席が空になり、着地後に新しい
  カードが入ることを実測。配布はスクリーンショットで「他席が1枚ずつ裏向きに
  着地 → 自分のカードも裏向きで着地 → 最後に表へ返る」順序を確認。
  コンソールエラーなし
  - 注意: 検証に使ったプレビュー枠はタブが`hidden`扱いで`requestAnimationFrame`が
    発火せずアニメーションが強く間引かれるため、**所要時間の実測値は当てにならない**。
    上記は時間に依存しない「順序」の確認

## v1.0.0b8 — 集計期間(戦績の期間管理)(試用版)

サーバー側の変更。**再起動が必要**(起動時に既存の成績DBを自動マイグレーション)。

### 追加

- **集計期間で戦績を区切れるようにした**: 成績を「期間」ごとに集計・表示し、
  管理コンソールから任意のタイミングで**〆る**と、そこまでの成績が締め切られて
  次の期間が空から始まる。ゼミの回や学期の区切りで戦績をリセットしたい、という
  運用のための機能
  - **〆ても記録は消えない**。締めた期間の対局記録はそのまま残り、コンソールの
    プルダウンや`--period <ID>`でいつでも見返せる。「リセット」されるのは
    表示される集計であって、データではない。記録そのものを消す既存の削除機能
    (メンテナンスタブ)とは別物なので、UI・ドキュメントの両方で誘導を分けた
  - 期間名は既定で`第N期`。〆る際に任意の名前(例: `2026年度後期`)を付けられる
  - 開催中の期間は常にちょうど1つ、という不変条件を`ensure_periods()`が接続の
    たびに保証する。ゲーム記録・評価サマリは記録時点で開催中だった期間のIDを持つ
- **管理コンソールの成績タブ**: 集計期間のプルダウン(開催中 / 全期間の通算 /
  〆済みの各期間)、期間名を添えた見出し、期間の一覧表、「開催中の集計を〆る」
  ボタンと次期間名の入力欄を追加。既定表示は**開催中の期間**なので、〆た直後の
  画面はそのまま新しい期間の成績になる
- **CLI**: `cucco.tools.stats`に`--periods`(期間一覧)と`--period <ID|current|all>`、
  `cucco.tools.admin`に`periods`と`close-period [--name]`を追加。stats出力の
  1行目にどの期間を集計しているかを表示する(〆の前後で数字が変わる理由が
  画面上で分かるように)
- **管理プロトコル**: `list_periods` / `close_period` / `rename_period` を追加。
  既存の`stats_*`アクションは`period`パラメータを受け取る(既定は`current`)

### 変更

- **既存の成績DBの取り込み**: 期間管理の導入前に記録された対局は、初回起動時に
  `第0期`(実際の記録範囲を開始・終了とする締め済みの期間)としてまとめて
  取り込まれ、新しい期間が空から始まる。取り込みは一度だけで、再起動しても
  重複しない。過去の成績はプルダウンで`第0期`を選べば従来どおり見られる
- **成績の全件削除**は集計期間の履歴も消し、新しい`第1期`から始めなおすように
  した(記録の残っていない期間がプルダウンに並び続けるのを避けるため)。日数
  指定の削除は期間の履歴に触れない

### 修正

- **管理コンソールで操作結果のメッセージが即座に消えていた問題**: 削除の完了
  メッセージを表示したあと`loadStorage()`が末尾で無条件に`showMsg("")`を呼んで
  いたため、「削除しました」が一瞬で消えていた。`stickyMsg`を尊重するようにして、
  削除と〆るの結果が読めるようにした

### テスト

- ユニット・結合テスト357件、全てパス(期間関連を新規13件追加:
  `tests/unit/persistence/test_periods.py` 10件、`tests/unit/server/test_admin.py` 3件)
  - 書き込みへの期間タグ付け、〆による期間の切り替わり、〆ても対局記録が
    残ること、期間フィルタの読み分け、全件削除での期間リセット、期間管理より
    前のDBの取り込み(一度だけ)、期間情報を持たないDBを読み取り専用CLIが
    そのまま読めること、管理アクションの引数検証
- ライブ検証(稼働中サーバーには触れず、本番DBの一貫スナップショットを別ポート
  8799/8800の使い捨てサーバーで検証): 実データ11,109件が`第0期`(2026-07-09〜
  2026-07-27)に取り込まれ、`period_id`が空のゲームが0件になること、新しい期間が
  空から始まること、コンソールでの期間切り替えと〆る操作、〆た直後に成績表が
  空になり第0期は11,109件のまま残ること、〆た後の実対局3件(mock AIの評価モード)が
  新しい期間に記録されること、評価サマリも期間で絞れること、不正な期間IDが
  エラーになることを確認

## v1.0.0b7 — 招待リンクのドメイン入力欄・チェックボックスの配置修正(試用版)

クライアントのみの変更(サーバー再起動は不要)。

### 追加

- **招待リンクに「ゲームサーバーのドメイン名」入力欄**: 招待リンクに載せる
  公開ドメインをその場で入力できるようにした。リンク欄は入力に合わせて即座に
  更新されるので、告知用URLを手で組み立てる必要がない
  - 主催者は自分のブラウザからは`localhost`で繋いでいることが多く、その接続先を
    そのままリンクに載せても外の人は繋げない。そのため**リンク用のドメインは
    自分の接続先とは独立**にした(この欄を変えても自分の接続先は変わらない)
  - 入力は`localStorage`(`cucco_invite_host`)に記憶するので、トンネルの
    ドメインが変わったときに1回入れ直すだけでよい。`https://`やパスを含めて
    貼り付けてもホスト名だけを取り出す。空にすると現在の接続先を使う

### 修正

- **チェックボックス・ラジオボタンがラベルからずれていた問題**: 「プレイヤー/
  観戦者」の選択や卓のルール設定(「馬/家どちらの拒否か公開する」)で、
  ボタンがラベルの文字列と別の行に離れて表示されていた。原因は
  `input, select { width: 100% }`というCSSがチェックボックス・ラジオにも
  当たっていたこと。これらを全幅指定から除外し、ラベル自体を横並び
  (flex)にしてボタンと文字列を同じ行に揃えた。ラベル全体がタップ領域になる
  ので、スマホでも押しやすくなる。**プレイ用・リファレンスの両クライアント**
  を修正

### テスト

- ユニット・結合テスト344件、全てパス(サーバー側の変更なし)
- ライブ検証: ドメイン欄への入力でリンクが更新されること、`https://`とパス
  付きの貼り付けからホスト名だけが取り出されること、リロード後も記憶されて
  卓の招待リンクにも反映されること、空欄で接続先にフォールバックすること、
  自分の接続先(`cucco_ws_host`)が書き換わらないことを確認。配置修正は
  ラジオ・チェックボックスの中心とラベル行の中心のずれが0px、幅が全幅から
  17pxになったことを実測(デスクトップ・375px幅の両方、両クライアント)

## v1.0.0b6 — 招待リンク(試用版)

URLを渡すだけで、接続先の設定も卓IDの入力もなしに参加できるようにした。
クライアントのみの変更(サーバー再起動は不要)。

### 追加

- **ゲームサーバー付きの招待リンク**(システムの紹介用): 名前入力後の画面に
  「Cuccoに招待するリンク」を追加。ランディングページ(クライアント選択画面)に
  `?ws=`(ゲームサーバー)を付けたURLをコピーできる。開いた人は接続先を設定
  せずにクライアントを選び、自分で卓を立てたり参加したりできる
- **この卓の招待リンク**(プレイヤーの勧誘用): 卓を立てたあとの待機画面に追加。
  `?ws=`+`?room=`(卓ID)を付けたURLで、**開いた人は名前を入れるだけでその卓に
  直接参加**する。名前入力画面には「卓 XXXXXX に招待されています」と表示する
- `?room=ID`をリファレンスクライアント(`/web/`)でも解釈するようにした。
  ランディングページは受け取ったクエリを両クライアントのリンクに引き継ぐため、
  招待された人がどちらのクライアントを選んでも卓に入れる
- リンクは読み取り専用の入力欄に表示し、隣の「コピー」ボタンでコピーする。
  クリップボードが使えない環境では自動でリンクを選択状態にして、その旨を
  表示する(共有モジュール`clients/web-common/invite.js`)

### 変更

- `?room=`は`?ws=`と同様、読み込み時に1回だけ使ってURLから取り除く。`ws=`は
  `localStorage`に保存されるが、`room=`はその訪問限りの指示にとどめる
  (リロード時は保存済みセッションの復帰を優先し、リンクの卓に入り直さない)
- 招待リンクの卓が既に終了していた場合は、生のプロトコルエラーではなく
  「卓がすでに終了している可能性があります。新しい招待リンクをもらうか、
  下のボタンから卓を作ってください」と案内してロビーに留まる

### テスト

- ユニット・結合テスト344件、全てパス(サーバー側の変更なし)
- ライブ検証: `?ws=`のみのリンクで接続先が設定されURLから消えること、
  `?ws=`+`?room=`のリンクで**別タブの新規参加者が名前入力だけで同じ卓に着席**
  すること(プレイ用・リファレンスの両クライアント)、ランディングページが
  両パラメータを引き継ぐこと、存在しない卓IDでは案内を出してロビーに
  留まること、コピーの結果表示が待機画面の再描画で消えないことを確認
- クリップボードへの書き込み自体は検証環境(プレビューブラウザ)では権限が
  下りず実行できていない。検証できたのは**失敗時のフォールバック**
  (リンクを選択状態にして案内を表示)の側。あわせて、書き込みが応答しない
  環境でボタンが無反応にならないよう1.5秒でフォールバックする処理を入れた

## v1.0.0b5 — カード効果の説明を修正(試用版)

サーバーの挙動は変更なし(表示テキストとドキュメントの修正)。再起動は不要。

### 修正

- **「人間」の説明の誤り**: カード効果パネル(？ボタン)で「山札から引かれた
  場合は**親**が失格する」となっていたのを「**引いたプレイヤー**(山札と交換
  する手番の人)が失格する」に修正。実装は当初から引いた本人を失格にしており
  (`Deal._disqualify(actor, cause="human_deck_draw")`)、説明文だけが誤って
  いた。山札交換は通常は親だが、親が途中失格して役割が引き継がれた場合や、
  馬・家の拒否連鎖が手番順の最後尾に達して**連鎖の要求元が山札と交換する**
  場合には、親以外のプレイヤーが引くことがある
- 同じ誤りをドキュメントでも修正: `docs/rules/final_rules.md`「特殊札の効果」
  表の人間・猫の行と列見出し、捨て札の行き先の記述。あわせて「親の山札交換に
  おける効果の連鎖」節の冒頭に、この節の「親」は「山札と交換する手番の
  プレイヤー」を指すという注記を追加
- `docs/rules/notes.md`(資料調査メモ)は資料の記述をそのまま残し、確定ルール
  では引いたプレイヤーが失格であることを注記として追記

### テスト

- ユニット・結合テスト344件、全てパス。**馬の連鎖で親以外が山札を引いた場合に、
  引いた本人(親ではない)が人間で失格する**ことを確認する回帰テストを追加
  (ドキュメントの記述を実装で裏付けるため)

## v1.0.0b4 — 人間によるテストプレイのフィードバック反映(試用版)

ゼミ内テストプレイで挙がった6点の指摘に対応。ルール(ドメイン)・プロトコル・
両クライアントにまたがる修正のため、**サーバーの再起動が必要**。

### 変更(ルール)

- **途中失格で残り1人になったディールは、その時点で終了する**: 単独生存者に
  カンビオ/ノンカンビオの選択肢を出さない。単独生存者は誰とも強弱を比べない
  ため敗者にならず勝敗は既に確定しており、手番を回しても無意味なばかりか、
  **山札交換で道化を引いて自ら失格する**不合理が起こり得た
  (`Deal.is_decided_by_disqualification`。判断は`docs/protocol/decisions.md`)

### 追加(プロトコル)

- **`presence_changed`イベント**: 席の接続状態が変わったことを他の在席者へ
  即時通知する(`player_id`, `connected`)。従来`connected`は
  `state_snapshot`にしか載っておらず、その配信はポット/ディールの境目に
  限られていたため、**リロードで復帰したプレイヤーの「切断」表示が次の
  ディールまで消えなかった**。シーン全体を作り直させない軽量な差分通知に
  したので、他プレイヤーの進行中アニメーションを壊さない

### 変更(プレイ用クライアント)

- **配布前に前ディールの捨て札処理を完了させる**: 新しいディールを配る前に、
  席に残っている前ディールのカードを捨て山へ飛ばして席を空にする。以前は
  配られたカードが前ディールの手札の上に重なって見えることがあった
  - あわせて空の手札スロットが幅を保つようにした(空のスロットは幅0に潰れ、
    飛行アニメーションの着地先として無効になってしまうため)
- **スマホでポップアップと操作ボタンが重ならないようにした**: 狭幅では
  バナー・確認カード・モーダルを卓の上部に寄せ、下部のアクションドックの
  領域を確保する(縦571pxの実測で、結果表示は従来76〜496pxでドック(472px〜)に
  重なっていたのが14〜435pxに収まる)
- **AIの参加人数を −/+ ボタンで設定**: 数値入力を廃止(スマホでは数値
  キーボードと極小の矢印を相手にすることになるため)。0未満・合計14人超は
  ボタンが無効化され、追加するAIの合計人数も表示する
- ランディングページのプレイ用クライアントから「開発中」表記を外した

### テスト

- ユニット・結合テスト343件、全てパス(残り1人ディールの新規テスト2件、
  presence通知の新規テスト1件を追加)
- 評価モードの統計テストは再試行回数を2→3に増やした。ディールが早く終わる
  ようになった分ゲームが短く・ばらつきが大きくなり(400ゲーム×10回の実測で
  counting_aggressiveの優位性は平均0.11・sd0.04・負値0回から平均0.09・
  sd0.08・負値2回へ)、優位性自体は保たれているが偽陰性率が上がったため
- ライブ検証: 捨て札スイープ(席4枚→捨て山へ飛翔→席0枚→山札から配布)を
  実対局で確認、presence通知はサーバー(両方向のブロードキャスト)と
  クライアント(切断/復帰の反映・重複無視)の双方を確認、AIステッパーと
  モーダル位置は実測で確認

## v1.0.0b3 — プレイ用クライアント: 捨て山を山の横に配置(試用版)

試用フィードバックを受けたレイアウト修正。サーバー側の変更はなく、公開
クライアント(GitHub Pages)への反映のみ。

### 変更

- **捨て山を「山」の横に配置**: 卓中央のレイアウトを見直し、山札とポットを
  縦に積み、その**横**に捨て山ボックスを置くようにした。これまでは捨て山が
  中央下(人間プレイヤーの席の近く)にあったため、横長画面では捨て札へ飛ぶ
  カードのアニメーションが**自分の手札に戻るように見える**ことがあった
- **捨て札の内訳をボックス内で折り返し表示**: 捨て札のランク別チップ(または
  捨て山モードの最上段カード)を、卓上に広がらず捨て山ボックスの枠内で
  折り返して表示するようにした。狭い画面では枠幅を縮めて `[山|捨て山]` が
  収まるよう調整

### テスト

- サーバー側の変更なし(プレイ用クライアントのみ)。ユニット・結合テスト
  341件、全てパス。ライブ検証: 横長(デスクトップ)・狭幅(モバイル375px)の
  両方で、捨て山が山の横に配置され内訳がボックス内で折り返すこと、捨て札へ
  飛ぶカードが自分の手札方向に見えなくなったことを実対局で確認

## v1.0.0b2 — プレイ用クライアント: 自動続行と確認モード3段階(試用版)

試用フィードバックを受けたプレイ用クライアントの操作性改善。サーバー側の
変更はなく、公開クライアント(GitHub Pages)への反映のみ。

### 追加

- **子の時間 自動続行トグル**: ONにすると、子供の時間(1〜3ディール目)の
  敗者に出る「続行しますか?」を、必ずチップを払って自動で続行するよう応答
  する(続行確認はそもそも子供の時間にしか出ないため、常に続行=trueでよい)。
  毎ディール確認を押さずに序盤を流したいときに
- **メッセージ確認モードを3段階に**(OFF / 最小 / フル): ツールバーの
  ボタンで循環切替
  - **最小**(新規): 失格・クク宣言など**ディールを左右する重要イベント**
    だけモーダルで確認を求め、アニメーションと効果音で状況が伝わる交換・
    スキップ・山札引き等は自動で流す。フルと従来OFFの中間
  - フル(従来のON相当)/ OFF(従来通り)
  - 旧設定(`cucco_confirm_mode` の "1"/"0")は自動でフル/OFFに移行

### テスト

- サーバー側の変更なし(プレイ用クライアントのみ)。ユニット・結合テスト
  341件、全てパス。ライブ検証: 3モードの分類(最小=非重要はフラッシュ・
  重要はモーダル、フル=全モーダル、OFF=全フラッシュ)を実対局とモジュール
  直接呼び出しで確認、自動続行が子供の時間の続行確認を自動応答することを確認

## v1.0.0b1 — 試用版(ゼミ内トライアル)

v0.18.0時点のコードを、ゼミ内で試用してもらうための区切りとして公開する
プレリリース。**コードの変更はなく**、バージョン表記とドキュメントのみ。
フィードバックを受けて調整したうえで v1.0.0 とする想定。

### この試用版に含まれるもの

- **ゲームルール**: 22種×2枚(44枚)の完全なルールエンジン(特殊札の連鎖・
  失格・拒否、子供の時間、ポットの持ち越し)
- **対局**: 人間同士 / 人間 vs AI / AI同士。卓作成時にサーバー内蔵AIを
  人数指定で同席させられる(基本3方策+カウンティング型2方策)
- **ブラウザクライアント2種**: プレイ用(卓レイアウト・カード/チップの
  アニメーション・効果音・カード効果パネル・メッセージ確認モード・
  捨て山表示・スマホ縦対応)とプロトコル最小実装のリファレンス
- **運用**: 放置卓の自動GC、安全なシャットダウン、ローカル管理コンソール
  (卓の監視・強制中止・成績確認・データ掃除)、成績確認CLI
- **記録**: SQLiteの成績とJSON Linesの行動ログ(決定論的リプレイ用)

### 試用でとくに見たいこと

- AIの強さのバランス(強すぎ/弱すぎ、面白さ)
- 確認モードを含むプレイ用クライアントの分かりやすさ・操作量
- タイムアウト設定(手番30秒など)の体感
- スマホでの見え方
- 進行が止まる・表示が食い違うなどの不具合

### 既知の制約(試用版時点)

- エンドポイントは無認証(公開範囲の限定が前提。`docs/security-notes.md`)
- 1ブラウザの複数タブで参加した場合、タブを閉じると最後のセッション以外は
  復帰できない(マルチセッション再接続は未実装)
- 成績の閲覧はサーバー機のローカル限定(管理コンソール / CLI)
- 行動ログは増え続けるため定期的な掃除が必要(管理コンソールで対応可)

## v0.18.0 — 管理コンソールに成績確認とメンテナンス機能

管理コンソールが3つのタブ(卓 / 成績 / メンテナンス)になった。

### 追加

- **成績タブ**: プレイヤー別の通算成績・内蔵AI方策別の成績・直近のゲーム
  結果(順位と獲得チップ)・評価モードの実行一覧をブラウザで確認できる。
  集計はCLI(`cucco.tools.stats`)と同一の`persistence/stats.py`を使い、
  **読み取り専用接続**なので稼働中のサーバーに対しても安全。表示名に実名が
  含まれ得る旨の注意書きを画面にも表示
- **メンテナンスタブ**: 保存状況(ゲーム記録数・参加記録数・評価モード記録
  数・DBサイズ・行動ログの件数と容量・最古/最新の記録日時)を表示し、
  データを削除できる
  - **成績の削除**: N日より古いもの / 全件。削除後に`VACUUM`を実行して
    ファイルサイズも縮小する
  - **行動ログの削除**: N日より古いもの / 全件(更新時刻で判定)
  - 成績と行動ログは独立して削除できる(ディスクを食うのはほぼ行動ログ
    なので、研究データの成績は残したままログだけ削れる)
- **管理アクションの追加**: `stats_overview` / `stats_recent` /
  `stats_evaluations` / `storage_info` / `purge_results` /
  `purge_action_logs`。管理リスナーに`results_store`と`action_log_dir`を
  渡すようにし、未設定の構成では明示的にエラーを返す
- `ResultsStore`に`storage_summary()`と`delete_results(before_iso=…)`を追加。
  削除は**サーバー自身の書き込み接続**を通すため、対局中の書き込みと競合して
  ロックすることがない

### 安全側の設計

- **全件削除は確認ダイアログに「削除」と入力しないと実行されない**(日数指定
  の削除は通常の確認ダイアログ)。実行した削除はサーバーログにも`WARNING`で
  記録される
- 削除前のバックアップを促す注意書きを画面と運用マニュアルに明記

### テスト

- 新規5件(成績集計の応答・保存状況・行動ログの日数指定/全件削除・成績の
  日数指定/全件削除・未設定時のエラーとlimit検証)。ユニット・結合テスト
  341件、全てパス。実ブラウザで成績表示と両系統の削除、および誤入力時に
  全件削除が中止されることを確認(検証は使い捨ての`--data-dir`に対して実施し、
  本番データには触れていない)

## v0.17.0 — サーバー管理コンソール(ローカルWeb UI)

### 追加

- **ブラウザ用の管理コンソール**: 管理リスナーが同じポートでHTMLコンソールも
  配信するようになった。サーバーを動かしているマシンで
  `http://127.0.0.1:8766/` を開き、管理トークンを入力すると使える
  - 卓の一覧を自動更新(2/5/15秒切替、停止可)で表示。状態(対局中・評価中・
    待機・終了)、参加人数と内蔵AI数、接続中の人間、観戦者、ポット番号、
    無操作時間、**無人時間**(GCの自動削除まであとどれくらいか)、作成時刻
  - 各卓から**詳細**(観戦者視点の状態。手札は含まれない)・**中止**・
    **削除**を実行。破壊的操作は確認ダイアログ付き
  - 管理CLI(`cucco.tools.admin`)は従来どおり利用可能で、同じ操作をする
- **卓サマリに`real_connected`を追加**: 「実クライアントが接続しているか」の
  即時判定。`real_absent_sec`はGCスイープ(60秒間隔)が初めて観測するまで
  `null`のため、それだけでは「在席」と「未計測」を区別できなかった。
  コンソールは在席/無人(計測中)/無人(経過時間)を正しく出し分ける

### セキュリティ設計

- コンソールのHTMLは**`clients/`ではなく`src/cucco/server/`に置く**
  (`clients/`はGitHub Pagesへ自動公開されるため、管理UIを置いてはいけない)
- **管理トークンはページに埋め込まない**(操作者が入力。ブラウザのセッション
  中のみ記憶)。ポートに到達できただけでは操作できない状態を保つ
- WebSocketハンドシェイク以外のHTTPリクエストには、コンソールのHTMLか404の
  みを返す(任意ファイルの配信はしない)。`Cache-Control: no-store`
- 管理ポートは従来どおり`127.0.0.1`固定で、**公開・トンネルに載せない**

### テスト

- 新規3件(コンソールHTMLの配信・WebSocketハンドシェイクの素通し・他パスの
  404、トークンが埋め込まれていないことの確認)。ユニット・結合テスト336件、
  全てパス。実ブラウザで接続・一覧・詳細・削除の拒否・中止までを検証

## v0.16.3 — 交換演出の順序修正+確認モードの重複モーダル整理

### 修正

- **馬/家のスキップ効果アニメーション完了前に交換結果が自席カードとして
  先行表示される不具合**: 権威値`yourHand`はイベント受信時に即更新される
  ため、スキップ連鎖の途中の`sync`で自席スロット・手札効果パネルが交換後の
  カードを早出ししていた。表示専用のミラー`shownHand`を導入し、交換/山札
  交換/配布の「カードを明かすステップ」でのみ前進させることで、効果
  アニメーションが完了してから新しいカードが現れるようにした
  (自席スロットと手札効果パネルの両方)

### 変更

- **確認モード: 手番プレイヤー自身のカンビオ成立モーダルを省略**。自分が
  手番で交換を選んだ場合、新しい手札は自席スロットで明かされるため
  「交換成立 — あなたの新しい手札」モーダルは冗長だった。省略対象は手番
  プレイヤー(要求者)のみで、他プレイヤーのカンビオで自分が交換対象に
  なった場合(自分から仕掛けていない)のモーダルは引き続き表示する

### テスト

- サーバー側の変更なし(プレイ用クライアントのみ)。ユニット・結合テスト
  333件、全てパス。ライブ検証: 馬/家スキップ17回で自席カードの先行表示
  違反0件、手番カンビオ53回で自席モーダル0件、交換対象時はモーダル表示を確認

## v0.16.2 — 放置卓の自動GC+安全なシャットダウン(無人運用向け)

### 追加

- **グレースフルシャットダウン**: `SIGTERM`(通常の`kill`)と`SIGINT`
  (Ctrl-C)を受けて、リスナーのclose・GCタスクの停止・クライアントへの
  going-awayフレーム送出を行ってから終了するようにした。従来はデタッチ
  起動だと`await asyncio.Future()`が`SIGINT`を取りこぼし、`SIGKILL`でしか
  止まらなかった。あわせてCtrl-C時の`KeyboardInterrupt`トレースバックも
  解消(SIGINTハンドラで抑止)。運用手順書に停止方法(`pkill -f`の`-f`が
  必須である点を含む)を追記

- **放置卓の自動ガベージコレクション**: サーバーが定期スイープ
  (`cucco.server.admin.run_gc_loop`)で放置卓を自動削除する。無人での仮運用
  を想定した機能。判定基準:
  - **実クライアントが誰も接続していない卓**が一定時間続いた場合(デフォルト
    10分)。判定は「無操作時間」ではなく「人間・観戦者・外部AIが一人も接続
    していない継続時間」で行う — **内蔵AIだけで連戦し続ける卓は常時ブロード
    キストしていて無操作時間が増えないため**、この基準でないと捕捉できない
    (v0.11.0以来の既知課題「観戦者が去った後もAIが連戦し続ける卓」を
    無人でも自動で掃除できるようになった)
  - **クラッシュ/終了して放置された卓**が一定時間アイドルの場合(デフォルト5分)
- `--gc-interval 秒`でスイープ間隔を変更、`--gc-interval 0`で無効化。手動の
  `abort`/`remove`(管理CLI)は即時掃除として引き続き利用可能
- 卓の管理サマリに「実クライアント不在の継続秒数」(`real_absent_sec`)を追加

### テスト

- 新規6件(実参加者判定・在席中は保持・無人graceでの削除・再接続での
  タイマーリセット・終了卓の削除・GCループが走行中のボット専用卓を停止して
  削除するE2E)。ユニット・結合テスト333件、全てパス

## v0.16.1 — メッセージ確認モードの修正+交換結果カード

### 修正

- **確認モードのモーダルが速すぎて押せない不具合を修正**: v0.16.0の確認
  モードで、確認カードが早送り(自分の手番到着時の`hurry`・結果画面の
  セーフティネット)によって強制的に閉じられ、確認ボタンを押す間もなく
  消えていた。確認モード中はこれらの自動早送りを無効化し、確認カードは
  クリックするまで待機するようにした
  - 結果パネルが確認カードを飛び越えて手前に出る問題(自分がポット外で
    AIが先行し複数ディールが裏で進むケース)も解消。結果パネルはキュー順に
    確認カードの後ろで表示される
  - ディール/ゲーム境界で、追いつけていない古い確認カードは破棄して
    現在のディールに同期(バックログの上限を保証)

### 追加

- **交換成立時に「あなたの新しい手札」の確認カード**: 確認モードで自分が
  関わる交換が成立したとき、手元に来たカードを1枚のモーダルで確認できる。
  特に馬/家のスキップが連鎖した後に最終的に受け取った札を見逃さない
  (スキップの各段はこれまで通り個別の確認カードになる)。通常モードでは
  追加のカードは一切出ない

### テスト

- サーバー側の変更なし(プレイ用クライアントのみ)。ユニット・結合テスト
  325件、全てパス。確認モードのライブ検証(確認カードのクリック待機・
  結果パネルの被りゼロ・スキップ連鎖・交換結果カード)を実施

## v0.16.0 — メッセージ確認モード+上位AIのクク即時宣言

### 追加

- **メッセージ確認モード(プレイ用クライアント)**: 右上ツールバーの
  「✋ 確認モード」でON/OFF(localStorageで永続化)。ONにすると進行
  メッセージ(拒否・スキップ・クク宣言・失格・離脱・山札交換などの
  バナー)が1枚ずつモーダルカードになり、確認ボタンを押すまで次の演出に
  進まない。初心者やゆっくり展開を追いたいときに
  - 確認待ちはアニメーションキューの既存fast-forward規則に完全に従う:
    自分のプロンプト到着・結果画面の安全弁・再接続の再構築で自動解除
    され、**サーバーのタイムアウトを確認モーダルが待たせることはない**

### 変更

- **上位AI(counting系)はククを持った瞬間に宣言する**: ククは交換を
  拒否できないため、自分の手番を待つ間に左隣のカンビオで奪われ得る
  (実対局で人間がAIからククを奪取する事例が発生)。counting_aggressive /
  counting_conservativeは配布時・交換で受け取った時に即座に
  `cucco_declare`を送るようにした(ファイア・アンド・フォーゲット仕様の
  本来の使い方)。基本3方策は従来どおりプロンプト時のみ宣言
  (評価ベースラインの互換維持)

### テスト

- 新規5件(即時宣言の発火条件・ディール内の重複抑制・ベースラインの
  従来動作維持)。ユニット・結合テスト325件、全てパス

## v0.15.0 — サーバー管理機能(AIロードマップ第4段階・完結)

### 追加

- **管理リスナー**(`cucco.server.admin`): 公開ゲームポートとは別の
  WebSocketリスナー(デフォルト`127.0.0.1:8766`、ローカルバインド固定)。
  起動ごとに管理トークンを生成しログに出力(`--admin-token`で固定、
  `--admin-port 0`で無効化)。**公開トンネルには載せないこと**
  (`docs/security-notes.md`に方針を明記)
- **管理CLI `python -m cucco.tools.admin`**:
  - `list` — 全卓の一覧(人数・内蔵AI数・接続中の人間・観戦者・ポット
    番号・無操作時間・作成時刻)
  - `status <卓ID>` — 卓の詳細(観戦者視点のsnapshot+セッション一覧。
    手札は含まれない)
  - `abort <卓ID>` — 進行中のゲームを強制終了して卓を閉じる。参加者には
    通常の`game_ended`(現在チップによる順位)が届き、内蔵AIのタスクも
    停止する
  - `remove <卓ID>` — ゲームの走っていない卓を削除(進行中なら拒否して
    abortを案内)
- **卓の無操作時間の記録**: クライアント操作・サーバー配信の両方で
  最終活動時刻を更新。「止まっている卓」発見の主指標
- v0.11.0以来の既知問題「**内蔵AIだけで連戦し続ける卓が自動終了しない**」
  はabortによる掃除で解消。未使用だった卓レジストリの削除経路も正規に
  機能するようになった

### テスト

- 新規6件(トークン拒否・一覧/詳細・実行中ボット卓のabortによるタスク
  停止と登録解除・removeの安全弁)+実ソケット経由のadmin E2E 1件。
  ユニット・結合テスト320件、全てパス

### 補足

- これでAIプレイヤーロードマップ全4段階(内蔵AI → カウンティング強化AI →
  成績CLI → サーバー管理)が完了

## v0.14.0 — 成績の記録と確認CLI(AIロードマップ第3段階)

### 追加

- **成績確認CLI `python -m cucco.tools.stats`**: サーバーが蓄積する結果DB
  (`data/results.db`)の読み取り専用ビューア。プレイヤー別の通算成績
  (対局数・勝利・勝率・平均順位・平均チップ)、`--policies`(内蔵AI方策別)、
  `--player 名前`(個人の直近ゲーム)、`--recent N`(直近ゲームの結果一覧)、
  `--evaluations`(評価モードの実行一覧)、`--mode`/`--db`。読み取り専用
  接続のため稼働中のサーバーに対して安全に実行できる
- **集計層 `src/cucco/persistence/stats.py`**: プレイヤーの横断識別は
  表示名(NFKC+casefold正規化 — 全半角・大文字小文字の表記ゆれを同一視、
  席の名前衝突チェックと同じ正規化)。旧スキーマの結果ファイルも
  フォールバックで読める
- **内蔵AIの方策名を成績に記録**: `participants`テーブルに`ai_policy`
  カラムを追加(冪等マイグレーション — 既存DBは次回サーバー起動時に自動で
  列追加)。方策別の勝率・平均順位の比較が正確にできるようになった

### 変更

- 評価モードの統合テスト(counting vs matrix)を400戦+1回再試行の
  統計テスト形式に変更し、フレークを解消

### 運用上の注意

- CLIの出力にはプレイヤーの表示名(ゼミ生の実名の場合がある)が含まれる。
  集計結果をファイル化しても**公開リポジトリにはコミットしないこと**
  (`docs/protocol/design.md`「永続化・成績記録」に明記)

### テスト

- 新規7件(名前集約・方策別集計・直近ゲーム・マイグレーション冪等性・
  旧スキーマ読み取り)。ユニット・結合テスト314件、全てパス

## v0.13.0 — 捨て札の「捨て山」表示モード

### 追加

- **卓作成時に捨て札の表示スタイルを選択可能に**: 従来の「全て一覧表示
  (種類ごとにまとめる)」に加え、「捨て山 — 最後の1枚だけ見える」を選択
  できる。`create_table`の`discard_display`(`grouped` | `pile`)として
  サーバーに渡り、`state_snapshot`で全参加者へ共有されるため、卓全体が
  同じ表示スタイルになる
- **プレイ用クライアント**: 捨て山モードでは卓中央に「捨て山 N枚」+
  最後に捨てられた1枚のカード面(重なりの影付き)のみを表示
- **リファレンスクライアント**も同期(フォームの選択肢と
  「捨て山(N枚)/トップ: X」表示)
- 位置づけは**表示上の卓設定でありルールではない**(配信データは不変。
  ドメイン層の`GameConfig`ではなくサーバー層の`Table`が保持)。
  `docs/protocol/design.md`に明記

### テスト

- パーサ検証と`state_snapshot`への伝搬の2件を追加。ユニット・結合テスト
  307件、全てパス

## v0.12.0 — カウンティング型強化AI(AIロードマップ第2段階)

### 追加

- **強化AI方策2種**: `counting_aggressive`(積極型)と
  `counting_conservative`(堅実型)。`docs/ai-advanced-policies.md`案A
  (カウンティング確率AI)+案D(チップ期待値)の参照実装
  (`src/cucco/ai/policies.py`の`CountingPolicy`)。卓作成フォーム・
  `ai_players`・mock_ai CLIのいずれからも選択可能
- **共有カウンティングトラッカー**(`src/cucco/ai/context.py`):
  捨て札(`deal_result`基準)・ディール中の公開情報(山札交換・即時開示
  失格・拒否時の開示札・オープン)・「持ち主が判明している札」の3層で
  未知カード集合を維持。二重計上を構造的に排除し、`deck_reshuffled`で
  リセット。判明札は交換成立で持ち主間を移動する
- **判断規則**: チェンジ判断は「未知札中の自分より弱い札の割合が
  カットオフ(人数の線形式)を下回るか」— 既存matrixのチェンジ基準表を
  一般化した式で、山がフレッシュならmatrixと一致し、カウンティングが
  進むほど判断が動的に変わる。確定判定(弱い札が全て場外→必ずチェンジ、
  相手の判明札が自分より弱い→ノーチェンジ)、人間・猫の残存密度による
  危険調整、子供の時間の消極化を含む。チップ継続は積極型(残1枚でも
  継続)/堅実型(次の敗北への備え+ポットの価値を要求)で分岐
- **方策インターフェースのコンテキスト対応**: `PolicyContext`を受け取る
  `decide_*_ctx`を追加。既存3方策・ゼミ生の旧2引数APIは完全互換

### 性能(評価モード300戦 vs matrix×3)

- `counting_aggressive`: 勝率0.277(基準0.25超)・平均順位2.34(場で最良)
- `counting_conservative`: 平均順位2.26・失格率0.473(いずれも場で最良。
  勝率よりチップ保全に寄る設計意図どおり)

### 修正

- 内蔵ボットの表示名が24文字の上限を超えると着席できない不具合
  (`AI-counting_conservative-1`で発生)。上限内に収まる採番に修正

### テスト

- 新規33件(トラッカー・判断規則・評価モードでの対matrix優位性)。
  ユニット・結合テスト305件、全てパス

## v0.11.0 — サーバー内蔵AIプレイヤー(AIロードマップ第1段階)

### 追加

- **卓作成時にAIプレイヤーを同席させられるように**: `create_table`の
  `ai_players`(例: `[{"policy": "matrix", "count": 2}]`)で、サーバー内蔵の
  AIプレイヤーを人数指定で着席させられる。別プロセスの起動は不要。
  対応方策は既存リファレンス実装の3種(`always_change` /
  `always_no_change` / `matrix`)。合計は最大参加人数-1まで(作成者の席を
  必ず確保)。未知の方策名は拒否
- **内蔵AIは対人用プロトコルの通常経路を通る**: インメモリのループバック
  接続で外部AIクライアントと同一の`ConnectionHandler`
  (identify → join_table → ready → 各宣言)を通過するため、名前検証・
  満席チェック・タイムアウト等の対人プロトコルがそのまま適用・検証される。
  表示名は`AI-<方策名>-<番号>`(既存参加者と衝突しない番号を採番)。
  着席は最初の参加者のjoin時(作成者不在での自動開始を防ぐ)。ゲーム終了後
  も退席せず再readyし、連戦に対応。観戦者が作成した卓はAIのみで自動開始
  (AI vs AI観戦)
- **両ブラウザクライアントの卓作成フォームに「AIプレイヤーを追加」欄**
  (プレイ用・リファレンスとも)
- **方策・ボット頭脳を`src/cucco/ai/`に一本化**: `clients/mock_ai/`は
  再エクスポートのシム+CLI輸送となり、内蔵AIと外部Mock AIクライアントが
  同一実装を共有する(既存のimportパス・ゼミ生のAIは変更不要)

### 修正

- **ゲーム終了の瞬間の切断で卓全体がクラッシュする不具合**: クライアントが
  イベント送信中にソケットを閉じると(ブラウザのタブを閉じる等)、runnerの
  ブロードキャストが例外になりテーブルが停止していた。送信失敗を
  「そのプレイヤーの切断扱い」に降格するよう修正

### テスト

- 新規15件(`ai_players`の検証、内蔵ボットの着席・自動開始・連戦・名前
  衝突回避、外部AI+内蔵ボット混在のE2E完走)。ユニット・結合テスト272件、
  全てパス

## v0.10.0 — プレイ用クライアント: カードデザイン刷新

### 追加

- **ローマ数字の数字札**: 数字札(1〜12)をセリフ体のローマ数字(I〜XII)で
  表示。ローマ数字が存在しない0はセリフ体の「0」。下部キャプションに
  算用数字を常に併記し、対応が一目で分かるようにした
- **名称札のオリジナルアイコン**: 道化・獅子・仮面・桶・家・猫・馬・
  人間・ククの9種に、完全オリジナルの幾何学的ピクトグラム(インライン
  SVG、`src/scene/cardArt.js`)を追加。ククのみ金色。外部画像は使わず
  ノービルド方針を維持。既存カードデッキの図柄は一切参照していない
- **マイクロインタラクション**: カードにホバーで浮き上がり、自分の札は
  タップで押し込み反応+約6秒周期の光沢スイープ演出。いずれも内側要素
  に限定し、既存のFLIP飛行・WAAPI演出とは干渉しない(飛行ゴーストでは
  無効化)。`prefers-reduced-motion`では停止
- **カード効果パネル・交換要求モーダルにカード面サムネイル**を表示

### 変更

- **カード裏面を刷新**: ストライプ+格子+中央メダリオンの重ねグラデー
  ション(CSSのみ)
- **自分の札をひと回り大きく表示**(60×84px、モバイル48×67px)
- 「↑最強」バッジをコントラストの高い濃い金色に変更

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト257件、全てパス。
  実対局(ボット2体)でデスクトップ/モバイル・ライト/ダークの4通りを
  目視確認

## v0.9.1 — プロジェクト全体の整合性レビュー

ドキュメント・プロトコル・実装の全体を突き合わせ、食い違いを修正。

### 修正

- **webリファレンスクライアントの不具合**: `isTerminalExchange`が拒否理由
  を`"human"`/`"cat"`で照合していたが、実際にサーバーが送るのは
  `"human_refusal"`/`"cat_meow"`。人間・猫による拒否が発生しても手番推測
  と宣言履歴(`declarations_this_deal`表示)が更新されない不具合を修正
  (プレイ用クライアントは元々正しい実装だった)
- **`ready`のドキュメント記述を実装に合わせて修正**: 「ポットごとに
  `ready`を再送し、タイムアウトで観戦扱いになる」という誤った記述を、
  実際の仕様(最初のゲーム開始のみをゲートし、後続ポットは自動復活で
  全員参加。最初の`ready`から10分の卓全体の安全弁)に訂正
  (`docs/protocol/design.md`、`docs/ai-client-guide.md`、
  `docs/human-client-guide.md`、`docs/web-client-operations.md`、
  `clients/mock_ai/mock_ai.py`のコメント)。あわせて使われていなかった
  `timers.py`の`"ready"`プロンプト種別を削除
- **`docs/protocol/design.md`のイベント表に欠落していた6件を追加**:
  `dealer_ready`(ユニキャストのプロンプト)、`chips_paid`、
  `continue_prompted`、`dealer_changed`、`pot_won`、`pot_wiped_out`
- 両クライアントガイドのフロー例で`turn_prompt`を「サーバー→全員」と
  誤記していた箇所をユニキャストに訂正
- `docs/protocol/design.md`のライフサイクル説明に、参加者全員(作成者
  含む)のreadyによる自動開始経路を追記
- human-client-guideの判断ポイント表に`effect_window`行が欠落していた
  ため追加
- `docs/web-client-operations.md`の古い「(開発中)」表記を削除
- READMEのテスト件数表記を実際の件数に更新

### テスト

- 使われていなかった`"ready"`プロンプト種別のテストケースを削除。
  ユニット・結合テスト257件、全てパス

## v0.9.0 — プレイ用クライアント: UIブラッシュアップ+スマホ縦対応

### 変更

- **ツールクラスタ**: 効果音トグルとカード効果ボタンを右上固定の1つの
  ツールバーにグループ化。ヘッダー情報(卓ID・ポット/ディール)は左寄せに
  変更し、効果音スイッチがポット表示に被る問題を構造的に解消
- **進行・手札効果を卓の余白へ**: 全幅の枠を廃止し、楕円卓の左下コーナーに
  進行メッセージ、右下コーナーに手札効果を半透明フローティングパネルとして
  配置。空いた分フェルトの表示領域が広がった
- **スマホ縦長画面へのレスポンシブ対応**(640px以下): コーナーパネルは
  卓とドックの間の全幅表示に自動で畳まれ、ツールはアイコンのみ、シート・
  カードは縮小、操作ボタンはタップしやすいサイズ(44px以上)、モーダルは
  幅94%に。`100vh`→`100dvh`でモバイルブラウザのUIバーによるはみ出しも修正

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト258件、全てパス

## v0.8.1 — プレイ用クライアント: 進行メッセージ・手札効果を見やすい位置へ

### 修正

- ゲーム進行メッセージ(ステータス行)と自分の手札の効果説明が、卓の
  アニメーション領域を挟んで画面最上部に離れて表示されており、自分の
  座席(卓の下側)や操作ボタン(画面最下部)から視線が離れて見にくかった
  問題を改善。両者を1つの枠にまとめ、卓とドック(操作ボタン)の間、
  つまり自分の座席・カードのすぐ上に移動した。進行状況・手札の意味・
  次に押す操作が同じ位置にまとまって見えるようになった

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト258件、全てパス

## v0.8.0 — プレイ用クライアント: 自分の手札の効果を常時表示

### 追加

- **手札効果ライン**: ステータス行の下に、今の手札の効果を一言で示す行を常設。
  例:「猫: 「ニャー」で拒否すると、交換相手の札の元の持ち主が失格します。」
  自分の手番でなくても常に見え、カード効果一覧パネルを開かなくても自分の
  札の意味を思い出せる。観戦者・手札未配布・失格後は非表示
- **交換要求モーダルにも反映**(宣言式ルールの卓): 平札を持っているときに
  届く「交換に応じる」だけのモーダルに「特殊効果はありません。交換には
  そのまま応じます。」を追記。特殊札の場合は既存の宣言ボタンの文言が
  説明を兼ねるため追記しない
- v0.7.0の`CARD_REFERENCE`データを流用し、数字札・桶・仮面・獅子の
  グループ表示を個別ランクへ展開する`cardEffectFor(rank)`を追加
  (一覧パネルと単一の出典を共有)

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト258件、全てパス

## v0.7.0 — プレイ用クライアント: カード効果説明パネル

### 追加

- **カード効果一覧パネル**(`clients/play/`): ヘッダー左上に「？ カード効果」
  ボタンを常設。押すと、道化を除く特殊札(クク・人間・馬・猫・家)と
  数字札・弱め特殊札(桶・仮面・獅子)・道化について、効果の説明と
  フレーバーテキスト(`docs/rules/flavor_text.md`で正式採用した宣言セリフ)
  を一覧表示するモーダルが開く。効果音トグルと同じ手法(`#screen`の外に
  マウント)で実装しており、対局中の画面再描画に巻き込まれず常時利用できる

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト258件、全てパス

## v0.6.1 — 接続先入力パネルが勝手に閉じる不具合を修正

### 修正

- 名前入力画面の「接続先を変更」パネル(`<details>`)が、開いて入力している
  最中にWebSocketの接続完了・再接続イベントなど裏側の状態変化をきっかけに
  勝手に閉じ、入力途中のホスト名も消えてしまう不具合を修正。原因は、この
  画面が状態変化のたびに`innerHTML`を丸ごと再構築していたため。パネルの
  開閉状態と入力中のテキストを再描画をまたいで保持するようにした
  (`clients/web/`・`clients/play/`両方)
- 実サーバーを落として再接続イベントを発生させ、パネルが開いたまま・
  入力済みテキストも保持されたまま再描画されることを両クライアントで確認

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト258件、全てパス

## v0.6.0 — クク宣言を非同期化(サーバーが保持者を待たない設計に変更)

v0.5.0で導入した`cucco_window`(手番外の合間にサーバーが保持者の応答を
待つ方式)を廃止し、**サーバーが誰の判断も待たない非同期宣言**に作り替えた
リリース。ウィンドウ方式は、応答を待つ間に卓の進行が止まること自体が
「クク保有者がいる」というタイミング・サイドチャネルになっていたため。

### 変更

- **`cucco_declare`をファイア・アンド・フォーゲット化**: クク札の保持者は
  ディール中いつでも(アトミックな処理中を除き)`cucco_declare`を送信でき、
  サーバーはこれをプロンプト応答としてではなく保留フラグとして受け取り、
  直近の安全点(アトミックな処理の切れ目)で検証・即時適用する。
  **サーバーが保持者の判断を待つ局面は一切なくなり**、卓の進行テンポは
  保持者の有無と完全に無関係になった
- **他プレイヤーの手番の考慮時間中でも宣言は即座に成立**する(考慮時間は
  アトミックな処理ではないため)。交換処理(連鎖・効果解決を含む)の
  最中に届いた宣言は、その処理の完了直後に適用される
- 親は「どうぞ」(`dealer_ready`)の場面で宣言可能。親以外の宣言が効力を
  持つのは「どうぞ」以降(それより前に届いた分は破棄せず、「どうぞ」直後の
  最初の安全点で適用される)
- **`cucco_window`イベント・`cucco_pass`アクションを廃止**。「宣言を見送る」
  ことは単に送らないことで表現され、専用のパス操作は存在しない
- **UIを常設ボタンに変更**: クク札を保持している間、ディール中は「クク宣言」
  ボタンが常に表示され続ける(自分の手番でも他プレイヤーの手番中でも同じ
  ボタン)。従来の割り込みモーダル(点滅+カウントダウン)は廃止

### テスト

- 他プレイヤーの考慮時間中に宣言が即座に割り込むこと(タイムアウトを待たず
  短時間で決着することを計測で確認)、親以外の「どうぞ」前宣言が「どうぞ」
  直後に適用されること、ククが手元を離れた後の無効な宣言が黙って破棄される
  ことを検証するテストを追加
- 既存のユニット・結合テスト258件、全てパス

## v0.5.0 — クク宣言のタイミングを整理(手番の第3の選択肢に対応)

クク宣言の仕組み自体(いつでも宣言可能、各アトミックな処理の合間に
`cucco_window`で機会を与える)は維持したうえで、宣言の入口を実態に
合わせて整理したリリース。サーバー・プロトコル・両クライアントに影響する。

### 変更

- **手番でクク宣言できるように**: `turn_prompt`が、カンビオ/ノンカンビオと
  並ぶ第3の選択肢として`cucco_declare`も受け付けるようになった。両クライアント
  のUIは、クク札を持つ手番でボタンが3つ並ぶ形になる(以前は手番中は
  `cucco_window`の割り込みモーダルでしか宣言できなかった)
- **親は「どうぞ」の場面で宣言**: `dealer_ready`への応答として`cucco_declare`
  を返せるようになった。親の通常の手番は最後尾に回ってくるため、他プレイヤーが
  動く前に宣言したい親にとってはこの場面が唯一の機会になる
- **親以外は「どうぞ」の前に宣言する機会を持たない**ように修正。従来はディール
  開始直後に親以外のクク保持者にも一括で`cucco_window`を送っていたが、その
  時点ではまだディールが始まっておらず手番も回っていないため、実態に合わせて
  廃止した。親以外の最初の宣言機会は「どうぞ」以降になる
- 上記に伴い、各アトミック処理後の`cucco_window`は、次に手番を行うプレイヤー
  (`turn_prompt`で自分で宣言できる)を除いて送信するようになった

### テスト

- クク宣言が手番の第3の選択肢として成立すること、親が「どうぞ」の代わりに
  宣言してディールが即終了すること、いずれのケースでも以降のプレイヤーへ
  手番が回らないことを検証するテストを追加
- 既存のユニット・結合テスト255件、全てパス

## v0.4.0 — プレイ用クライアント: 進行を追える演出ペーシング

特殊札の効果(スキップ・山札引き・失格など)を、卓の全員が「誰が・
どうして」を追えるように、1枚ずつ・理由付きで見せるよう作り直した
リリース。プロトコル・サーバー側の変更はなし。

### 追加

- **判定結果パネルの表示タイミング制御**: 結果パネルを演出キューの最後に
  積むようにし、クク公開・効果モーション・オープンのめくりといった演出が
  再生し終わってから表示するように変更。演出とパネルが被らなくなった
  (演出が長引いた場合の安全網として、一定時間経過後は残りを即時完了させ
  パネルを必ず表示する)
- **山札から引いたカードを全員に公開**: カンビオでの山札交換・山札引きの
  際、引いたカードを**表向き**で山札から手元へ運び、「◯◯ が山札から △△ を
  引く」と明示。物理卓同様に全員が引いたカードを見られるようになった
  (従来は裏向きで、引いたカードが分からなかった)
- **失格の理由を明示**: 「◯◯ 失格」だけでなく「◯◯ 失格 — 人間に拒否された」
  のように原因を併記。開示設定が遅延(カード非公開)の場合も、誰がどの理由で
  失格したかを必ず伝える
- **スキップ・拒否の状況を全員に**: 拒否した札を対象の席で表向きにし、効果
  モーションと「◯◯: 猫「ニャー!」」等のバナーで、誰がどの札で拒否したかを
  はっきり見せる

### 変更

- 特殊札の効果演出を「カードが動く → 表になる → 読む間(ま) → 決着」の
  ビートに整理し、1枚ずつ処理の過程を追えるようにペースを調整
- 自分の手番プロンプト受信時、これまでは演出を即座に飛ばしていたが、
  代わりに残りの演出を早送りで見せる方式に変更(直前に他プレイヤーの身に
  何が起きたかを見てから判断できる)。操作ボタンは即時有効なため待たされ
  ない。演出が停滞した場合は上限時間で残りを即時完了する

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト253件、全てパス

## v0.3.2 — プレイ用クライアント: 透過率の再調整・未演出アクションの補完

### 追加

- **ノンカンビオ宣言**(`no_change`): 効果音("pass")と、対象の手札スロットに
  控えめな確認パルス演出を追加。これまで無音・無演出だった
- **ポット離脱**(続行確認で離脱を選んだ場合、`left_pot`): 効果音("leave")
  と離脱バナーを追加。これまで無音・無演出だった

### 修正

- 判定結果(ポット結果/ディール判定結果)のモーダルペインの透過率を
  さらに上げ(背景アルファ0.88→0.6、背後のオーバーレイ0.32→0.15)、
  卓の様子がよりはっきり透けて見えるように調整

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト253件、全てパス

## v0.3.1 — プレイ用クライアント: 演出タイミング・判定結果ペインの微調整

### 修正

- 山札から引く演出(カンビオでの山札交換・拒否)のカードが山札から手元へ
  移動する時間が短く見づらかったため、450ms→750msに延長
- 判定結果(ポット結果/ディール判定結果)のモーダルペインが不透明で
  卓面が完全に隠れていたのを、半透明(背景アルファ+ぼかし)にして卓が
  少し透けて見えるように変更。背後のオーバーレイの暗さも軽減。ゲーム
  終了モーダルは対象外(従来通り不透明)

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト253件、全てパス

## v0.3.0 — プレイ用クライアント: 効果音・カード演出

v0.2.0のプレイ用クライアント(`clients/play/`)に、効果音と特殊札の
発動演出を追加したリリース。プロトコル・サーバー側の変更はなし。

### 追加

- **効果音**(`clients/play/src/anim/sound.js`): Web Audio APIで合成した
  効果音(音声ファイル不使用)。手番・配布・交換・クク宣言・失格・
  ポット獲得などの主要な局面で再生。ON/OFFはヘッダー右上の常設トグル
  ボタンで切り替え、`localStorage`に永続化。効果音はプレイ用クライアント
  のみに実装(リファレンス`clients/web/`・`clients/web-common/`には一切
  含まれない)
- **特殊札の発動演出**: 猫・人間・馬/家・クク宣言・道化(失格)それぞれに
  専用のカードモーション(`clients/play/src/anim/effects.js`)。効果対象の
  カードは、卓ルール(`horse_house_reveal`等)に応じてサーバーが公開する
  タイミングでflipして表向きになり、モーションを再生する

### 修正

- 効果音ON/OFFトグルのUIが、小さな無地の丸アイコン(38px)で視認しづらく
  見落とされやすかった問題を修正。「🔊 効果音 ON」/「🔇 効果音 OFF」の
  文言付きピル型ボタンに変更し、一目でわかるようにした

### テスト

- サーバー側の変更なし。既存のユニット・結合テスト253件、全てパス
- 実サーバー+ブラウザでの効果音再生・トグル操作・カード演出のライブ検証
  を実施

### 今後の予定

- 成績・統計の閲覧UI
- 評価モード専用の閲覧ダッシュボード
- タブを閉じた後の複数プレイヤー個別復帰(現状はベストエフォート)

## v0.2.0 — 人間向けブラウザクライアント(リファレンス+プレイ用)

v0.1.0のサーバー・プロトコルはそのままに、人間向けブラウザクライアントを
2種実装し、学外公開運用まで実際に検証したリリース。

### 追加

- **リファレンスクライアント**(`clients/web/`): プロトコル全機能を最小限の
  UIで実装したビルドツール不要のブラウザクライアント。名前入力→卓作成/
  参加→対局→観戦→ゲーム終了まで一通り対応。プロトコル変更時の実装例・
  検証用として今後も保守する
- **プレイ用クライアント**(`clients/play/`): 卓を囲む楕円レイアウト(自席
  常に下中央)に、カード配布・交換・山札交換・オープン・チップの支払い/
  獲得・拒否/クク宣言/失格・山札再構築のアニメーションを備えたリッチ
  クライアント。状態更新は即時・正、演出はプレゼンテーションキューで逐次
  再生し、**自分宛のプロンプト受信時は演出を即座に完了させて操作を優先する**
  設計(サーバーのタイムアウトは演出を待たないため)。リファレンスと共有
  モジュール(`clients/web-common/`)を介して同一プロトコル処理を利用し、
  同一卓で相互に対局できる
- **卓ルールの拡張**: 結果確認の待機時間(`result_pause_sec`、全員確認で
  スキップ可能)、特殊札(道化を除く)の効果を能動的な宣言制にする
  `effect_declaration: "declared"`ルール、着席順・各ポット最初の親の
  ランダム化、卓作成者主導のゲーム開始(`start_pot`)と切断時の主催者
  自動引き継ぎ、ゲーム終了後の同一卓での連戦(チップリセットして再開)
- **公開運用**: GitHub Pages(`clients/index.html`のランディングページ経由で
  両クライアントを配信)+ Cloudflare Tunnelでの学外限定公開を実運用で検証
  (`docs/web-client-operations.md`)。セキュリティ上の残存リスクを整理した
  `docs/security-notes.md`を追加し、入力検証(表示名の長さ・制御文字・
  重複判定の正規化)を強化

### 修正

- 再接続まわりの複数の不具合(切断検知の競合、手番中の再接続でプロンプトが
  再送されない、ポット境界での再接続猶予、複数タブ間のセッション混線)
- 山札から引いたカードが本人にしか配信されていなかった問題(物理卓では
  公開情報であるべき)
- 結果確認の一時停止が、ポット決着時のブロードキャスト順序により結果
  データが空になっていた問題

### テスト

- ユニットテスト・結合テストあわせて253件、全てパス(v0.1.0比+47件)
- 複数回のFable5(独立エージェント)によるレビュー・実サーバーでのライブ
  検証(bot対局・複数タブ・トンネル経由の実機接続)を経て確定

### 今後の予定

- 成績・統計の閲覧UI
- 評価モード専用の閲覧ダッシュボード
- タブを閉じた後の複数プレイヤー個別復帰(現状はベストエフォート)

## v0.1.0 — 初期リリース

Cuccoの要求仕様策定からルールエンジン・プロトコル・サーバー・永続化・評価モード・
サンプルクライアントまでの初期実装一式。

### 追加

- **ドメイン層**(`src/cucco/domain/`): 22種×2枚のカード構成、特殊札(道化・人間・
  馬・猫・家・クク)の連鎖・拒否・失格ロジック、ディール/ポット/ゲームの進行管理。
  I/Oを一切含まない純粋な同期エンジン
- **プロトコル層**(`src/cucco/protocol/`): `{type, table_id, protocol_version,
  payload, ts}`形式のJSONエンベロープ、アクション/イベントの相互変換
- **サーバー層**(`src/cucco/server/`): WebSocketによる非同期対局進行、卓管理、
  再接続対応、タイムアウト・切断時の自動処理
- **永続化層**(`src/cucco/persistence/`): ゲーム終了ごとの成績をSQLiteに記録、
  シャッフルシード+行動ログをJSON Linesで保存し決定論的リプレイに対応
- **AI専用高速評価モード**(`src/cucco/evaluation/`): `game_count`回のゲームを
  座席ローテーションしながら自動連続実行し、勝率・平均順位・平均チップ・失格率
  などの集計結果を配信
- **卓ごとのルール設定**: 終了条件(チップ0枚/既定ラウンド数)、特殊札ごと
  (道化/人間/猫)の失格カード開示タイミング(即時/遅延、一括設定も可)、馬・家の
  拒否時の開示可否を`create_table`時に卓単位で選択可能
- **サンプルクライアント**(`clients/`): Mock AI(必ずチェンジ/必ずノーチェンジ/
  人数×ランクのマトリクスの3方策)、対話式ターミナルStubクライアント
- **ドキュメント**(`docs/`): 要求仕様、確定ルール、プロトコル設計、AI/人間向け
  接続ガイド、上位AI実装案集(教材)

### テスト

- ユニットテスト・実WebSocket経由の結合テストあわせて206件、全てパス
- 複数回のFable5(独立エージェント)によるコードレビューを経て主要な不具合を修正

### 今後の予定

- 人間向けUI(ブラウザ等、別プロジェクト)
- iMacでの常時運用 + Cloudflare Tunnel経由の限定公開
- 成績DBの参照用クエリスクリプト
